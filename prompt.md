# 主动推理世界模拟器——开发规划与迭代任务

## 最终目标
构建一个端到端系统，对外提供单一 API：
- **输入**：自然语言指令（例如：“给我一个老虎的生命历程”）
- **输出**：
  - 一段连贯的视频，展示老虎从出生到死亡的完整生命过程；
  - 同时附上逐帧/关键事件的文字描述（生命历程叙事）。

底层由主动推理世界模型驱动，智能体（老虎）通过最小化变分自由能在模拟世界中自主生存、成长、捕食、繁殖直至死亡。所有状态演进经渲染引擎生成视觉帧，并结合状态语义生成解说文字。

## 总体技术路线
系统分为六个核心模块，按依赖关系分阶段迭代开发。每个阶段产出可独立测试的组件，最终在阶段 6 整合为终端 API。

### 模块依赖图
Stage 0: 生成模型接口与数据结构
↓
Stage 1: 世界模拟器 + 推断引擎 (主动推理循环)
↓
Stage 2: 语言解析与场景构建 (NLP → 初始状态/偏好)
↓
Stage 3: 智能体生命模型 (生存、成长、繁殖、死亡行为)
↓
Stage 4: 视觉渲染器 (状态 → 视频帧)
↓
Stage 5: 文字叙事生成 (状态轨迹 → 文本解说)
↓
Stage 6: 端到端 API 集成 (文本进，视频+文本出)

---

## Stage 0: 生成模型接口与数据结构 (已体现在 design doc 中)
**目标**: 定义并实现生成模型的全部抽象接口、状态空间、域对象，确保任何物理规律都能以统一概率形式接入。

**关键任务**:
- 实现 `StateSpace`, `ObservationSpace`, `ActionSpace` 等数据结构。
- 定义 `GenerativeModel` 抽象基类，包含 `get_observation_model()`, `get_transition_model()`, `get_preferences()`, `get_initial_belief()` 等方法。
- 实现 `Distribution` 抽象及具体离散/连续子类 (`Categorical`, `MultivariateGaussian`)，支持 `sample` 和 `log_prob`。
- 提供 `LikelihoodModel`, `TransitionModel` 封装类。
- 单元测试覆盖各分布、模型序列化。

**交付物**: `generative_model.py`, `distributions.py`, 测试用例。

---

## Stage 1: 世界模拟器与推断引擎 (核心主动推理循环)
**目标**: 实现环境真实状态更新与智能体信念更新、策略选择的闭环，能够进行简单的自维持行为（如恒温器、简单导航）。

**关键任务**:
- 实现 `WorldSimulator` 类，严格遵循隐藏真实状态，仅暴露 `step(action) -> obs` 和 `observe()`。
- 实现 `InferenceEngine` 基类，包含 `infer_state`, `compute_efe`, `plan_action`。
- 离散状态版本：使用贝叶斯滤波器进行精确后验推断，策略选择基于暴力搜索或 MCTS。
- 连续状态版本：实现扩展卡尔曼滤波或粒子滤波作为变分推断后端。
- 实现一个简单物理世界示例（如粒子阻尼运动 + 偏好原点），验证智能体能通过行动维持期望状态。
- 记录轨迹 (状态、行动、观测、自由能)。

**验收标准**: 在恒温器场景中，智能体成功通过调节行动将温度维持在偏好区间；自由能随时间降低。

**交付物**: `world_simulator.py`, `inference_engine.py`, 示例 `thermostat.py`，对应测试。

---

## Stage 2: 语言解析与场景构建
**目标**: 使系统能够理解自然语言描述的命令，并自动将其转化为生成模型的初始状态、偏好分布、物理参数等。

**关键任务**:
- 定义场景模板库：包含常见场景要素（地形、物体、生物种类、生命阶段偏好等）。
- 利用大语言模型（LLM）或简单规则解析器，将用户文本映射为结构化场景描述 JSON：
  ```json
  {
    "biome": "jungle",
    "agent": {"type": "tiger", "life_stage": "birth", "preferences": {...}},
    "duration": "lifespan",
    "entities": ["prey", "water_source", "trees"],
    "physics": "default_ecology"
  }
  实现场景构建器，根据结构化描述实例化 GenerativeModel 的具体子类，配置初始状态（位置、健康值、年龄等）和偏好目标（摄食、安全、繁殖等）。

对此解析流程进行单元测试，确保“老虎的生命历程”稳定映射为正确的场景定义。

验收标准: 对“老虎的生命历程”的解析能可靠生成包含老虎幼崽、偏好猎物、水源、0岁初始年龄等参数的世界配置。

交付物: language_parser.py, scene_builder.py, 模板文件 scene_templates.json。

Stage 3: 智能体生命模型
目标: 在模拟器中实现足够真实的老虎智能体，其行为由主动推理驱动，能展现出生、成长、捕食、繁殖、衰老及死亡的全生命周期。

关键任务:

设计老虎的状态因子：位置、饥饿度、健康值、年龄、繁殖状态、位置等。

设计动作空间：移动、饮水、捕猎、进食、休息、交配、育幼等。

动态偏好模型（C 随时间/状态变化）：根据内部驱动力调整，如饥饿驱动更倾向于进食，繁殖季节偏好交配。

实现老虎与生态系统的交互（猎物、水资源），猎物同样可作为简单主动推理体或根据固定规则响应。

环境转移模型包含：新陈代谢导致饥饿上升、健康随年龄衰减、衰老导致死亡率增加、捕食成功概率等。

使用 InferenceEngine 让老虎在生态圈内自主行动；定义生命周期关键事件触发器（如健康值归零死亡）。

批量模拟生成生命历程轨迹，验证能稳定复现典型的野生虎生命阶段序列。

验收标准: 多次运行的模拟中，老虎能够成长到成年，成功捕食，有机会繁殖，并最终因衰老或被饿死/伤病而死亡；整个生命历程没有卡死或极端失真。

交付物: tiger_ecology_model.py (继承 GenerativeModel)，老虎的偏好与驱动力更新逻辑，生态世界配置，示例轨迹记录。

Stage 4: 视觉渲染器
目标: 将模拟产生的状态轨迹转换成连贯的视觉帧序列，合成为视频。

关键任务:

定义场景到视觉元素的映射：地形贴图、植被、水体、动物模型（老虎、猎物等）。

实现一个轻量级渲染器（Python + Matplotlib/Pygame 或更高级的如 Ursina/小型游戏引擎嵌入），每帧根据当前世界状态绘制画面。

老虎状态映射为动画精灵位置、姿势（行走、捕食等）。

环境状态：白天/夜晚周期、天气（可选）。

生成画布序列，使用 imageio 或 cv2 导出为视频文件（如 mp4）。

支持配置分辨率、帧率。

单元测试：给定预计算的状态序列，输出可播放的视频文件。

验证: 输入一段模拟轨迹，生成短时视频，视觉无闪烁，实体位置连续。

交付物: visual_renderer.py，依赖的基本图形资源库。

Stage 5: 文字叙事生成
目标: 为状态轨迹生成自然语言的生命历程描述，可与视频时间戳对齐。

关键任务:

从模拟轨迹中检测关键事件：出生、首次饮水、首次捕猎、受伤、繁殖、后代出生、死亡等。

设计叙事模板，基于事件和状态特征（年龄、体重变化、地点）生成描述性句子，如：“3岁时，它成功捕获了一头水鹿，体重增长了 20 公斤。”

可选：使用 LLM 润色模板文本，使叙述更连贯。

将叙事文本与时间轴关联，输出 JSON 格式 [{"time": 0, "text": "..."}]，方便视频字幕集成。

验证“老虎的生命历程”输出能形成有信息量、符合生命逻辑的完整故事。

验收标准: 对于一次完整的生命模拟，生成的文字描述覆盖出生、成长、捕猎、繁殖、死亡全过程，句意明确，无歧义。

交付物: narrative_generator.py，事件检测模块。

Stage 6: 端到端 API 集成
目标: 封装所有模块，提供最终的文本到视频+文字的单一 API。

关键任务:

实现 LifestoryAPI 类，提供方法 generate(prompt: str, output_video_path: str) -> dict，返回包含视频文件路径、文字叙事、元数据的响应。

内部流程：

使用 Stage 2 解析器将 prompt 转为场景配置。
实例化对应的 GenerativeModel 和 WorldSimulator。
循环运行主动推理直到触发终止条件（死亡或设定最大步数），记录完整轨迹。
调用 Stage 4 渲染器生成视频。
调用 Stage 5 叙事生成器生成文字脚本，并嵌入视频字幕。
返回视频和文字内容。
添加错误处理、超时控制、日志记录。

编写集成测试，确保输入“给我一个老虎的生命历程”能在合理时间内返回可用结果。

最终验证: 给定 prompt “给我一个老虎的生命历程”，API 能在 10 分钟内（或可接受时长）生成一个时长 2-5 分钟的视频，其中展示幼虎出生、成长、捕猎、衰老、死亡过程，并伴随准确的中文描述字幕。

交付物: life_story_api.py, 完整的 requirements.txt, 系统部署指南。

迭代里程碑总结
阶段	核心产出	验证方式
0	生成模型接口	单元测试通过
1	闭循环主动推理	恒温器自维持实验
2	自然语言到场景	“老虎的生命历程”解析为正确配置
3	老虎生态模型	多次模拟有完整生命阶段
4	视频渲染	静态轨迹生成连续视频
5	文字叙事	叙事覆盖关键事件
6	端到端 API	输入文字，输出视频+故事文本

开发建议
迭代开发时，每完成一个阶段都必须做回归测试，保证前期模块仍能通过。

Stage 3 和 Stage 4 可部分并行开发，因为视频输出不依赖于最终的行为复杂度，可先使用简化生命模型渲染占位视频以验证管线。

在 Stage 5 可先使用规则模板，后续再集成 LLM 增强叙事质量。

关键承诺：当所有阶段完成之后，调用最终 API generate("给我一个老虎的生命历程")，将直接得到一个老虎从出生到死亡的全过程视频，以及同步的、详尽的生命历程文字描述。系统底层完全由主动推理的生成模型驱动，智能体在模拟世界中自主维持其生存边界，直至不可避免的衰竭——这正是主动推理“自维持”概念的直观展现。
